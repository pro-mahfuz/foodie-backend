server:
  port: ${SERVER_PORT:8081}
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://user-db:3306/user_db?serverTimezone=UTC}
    username: ${DB_USER:foodie}
    password: ${DB_PASSWORD:foodie}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
